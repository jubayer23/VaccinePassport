<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sprytech.vaccinepassport">



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeNoActionBar">


        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->

        <activity
            android:name="com.sprytech.vaccinepassport.ui.registration.RegistrationConfirmationActivity"
            android:label="@string/title_activity_how_it_works">

        </activity>

        <activity
            android:name="com.sprytech.vaccinepassport.ui.registration.howItWorks.HowItWorksActivity2"
            android:label="@string/title_activity_how_it_works">

        </activity>
        <activity
            android:name=".ui.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppThemeNoActionBar.NoActionBar" />
        <activity android:name=".ui.AuthActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>